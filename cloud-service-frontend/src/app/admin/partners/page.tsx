import PartnerRequestsPage from '@/app/admin/partner-requests/page';

export default function AdminPartnersPage() {
    return <PartnerRequestsPage />;
}

// PartnerRequests is the canonical affiliate workflow because the public
// partner registration form submits to /api/PartnerRequests.
